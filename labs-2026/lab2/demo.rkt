#lang racket

; (func <param1> <param2> ...)

; DEMO: define numerical constants and bind expressions to variables in code (e.g. a = 10, b = a + 1). Talk about immutability in FP.

; DEMO: define simple function

; DEMO: write simple recursive function in Racket (e.g. factorial, doubling a list etc)

; DEMO: if, cond, logic functions (and, or) and what is special about them.

; DEMO: talk about typing in Racket.

; DEMO: lambda functions

; DEMO datatypes: numbers, functions, booleans, lists, characters, symbols (example on ' syntax and semantics)

; DEMO: lists seen as pairs (cons and null). Useful functions: null?, length, append, cons, null, list

; DEMO: useful functions & Racket doc

; DEMO: equal? vs eq? vs =

; DEMO (advanced): eval && metaprogramming